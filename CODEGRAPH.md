# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-08 16:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a FastAPI-based web API backend that serves as an orchestration layer for AI-driven scene and image generation workflows. It manages several interconnected domains: projects (top-level organizational units), assets (files/resources associated with projects), character definitions with associated LoRA model references (fine-tuned AI model weights tied to specific characters), image generation jobs (requests submitted to and tracked through AI inference workers), person tracking data (likely bounding-box or pose annotations on generated or uploaded images), and model manifest management (tracking available AI models and their metadata). The single project in the repository appears to be a unified backend service rather than a multi-project monorepo. It exposes REST endpoints consumed by a frontend client and dispatches generation work to underlying AI inference workers, acting as the coordination point between user-facing requests and compute-side execution. No cross-repository dependencies were identified, but the system implicitly depends on external AI inference infrastructure (e.g., diffusion model servers or LoRA-capable generation backends) and likely a relational database for persistence. Confidence is medium because the analysis is based on API structure and parameter patterns rather than full implementation details.

## Projects

### SceneWorks

SceneWorks is a web API backend (built with FastAPI based on the Request parameter patterns) that manages AI-driven scene and image generation workflows. It handles projects, assets, character definitions with LoRA model associations, image generation jobs, person tracking data, and model manifest management — acting as the orchestration layer between a frontend client and underlying AI inference workers.

