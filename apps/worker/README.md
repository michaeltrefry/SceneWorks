# SceneWorks Worker

Placeholder worker package for future generation, download, tracking, replacement, extraction, and export jobs.

The worker currently prints a startup readiness payload and then emits heartbeat payloads that match the fields expected by the future dispatcher.
