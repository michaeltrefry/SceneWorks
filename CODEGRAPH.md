# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-23 11:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a FastAPI-based web API service that orchestrates AI-driven image and scene generation workflows. The system manages a data model centered around projects, which contain assets, characters (associated with LoRA model weights), and image generation jobs. It coordinates background worker processes for job execution, maintains a jobs store for tracking generation state, and exposes server-sent events (SSE) endpoints for pushing real-time status updates to clients. It also handles person tracking data and AI model manifests, suggesting it integrates with or manages multiple AI model backends. The repository appears to be a single-service backend — no cross-repository dependencies were identified — serving as the control plane for a generative AI content pipeline, likely consumed by a frontend client or orchestration layer that subscribes to its SSE streams and submits generation requests.

## Projects

### SceneWorks

SceneWorks is a web API service (built with FastAPI based on the Request parameter patterns) that manages AI-driven scene and image generation workflows. It handles projects containing assets, characters with LoRA model associations, image generation jobs, person tracking data, and AI model manifests. The API coordinates background workers, maintains a jobs store, and serves server-sent events (SSE) for real-time updates to clients.

